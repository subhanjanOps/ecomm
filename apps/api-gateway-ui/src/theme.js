import { createTheme } from '@mui/material/styles'

// A simple custom theme for the gateway UI
const theme = createTheme({
  palette: {
    primary: {
      main: '#1976d2',
    },
    secondary: {
      main: '#9c27b0',
    },
  },
})

export default theme
